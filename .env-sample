# DB2 Database Configuration
### Password for the DB2 instance administrator
DB2INST1_PASSWORD=my-db2-password  # Replace with your desired DB2 instance password

### DB2 instance name
DB2INSTANCE=db2inst1  # Default instance name; change if needed

### Name of the database to be created or used
DBNAME=TESTDB  # Replace with your desired database name

### Port for DB2 to communicate with
DB2_PORT=50000  # Default DB2 port; adjust if needed

# Spring Boot Backend Configuration
### Toggle SSL for the server
SERVER_SSL_ENABLED=false  # Use "true" to enable SSL, otherwise "false"

### Backend server port
SERVER_PORT=8443  # Port on which the Spring Boot application will run inside the container

### JWT secret for authentication
JWT_SECRET=to-mod  # Replace with a secure secret key for JWT

### Database connection password for Spring Boot
SPRING_DATASOURCE_PASSWORD=my-db2-password  # Use the same password as DB2INST1_PASSWORD

### JDBC URL for connecting Spring Boot to the DB2 database
SPRING_DATASOURCE_URL=jdbc:db2://db2instance:50000/TESTDB  # Update DB2 hostname, port, or database name as needed

### DB2 username for Spring Boot
SPRING_DATASOURCE_USERNAME=db2inst1  # Use the same username as your DB2 instance

### Hibernate setting for managing database schema
SPRING_JPA_HIBERNATE_DDL_AUTO=update  # Options: validate, update, create, create-drop; use based on your needs

### Base API URL for the backend application
SPRING_BOOT_API_URL=http://localhost:8444  # Base URL where the backend will be accessible

### Port mapping for the backend (container to host)
BACKEND_PORT=8444  # Port to expose the backend on the host system

# Frontend Configuration
### Port mapping for the frontend (container to host)
##### Development Stage
FRONTEND_DOCKERFILE=Dockerfile.dev
FRONTEND_CONTAINER_NAME=frontend-dev
FRONTEND_COMMAND="npm run dev"
FRONTEND_PORT_MAPPING=5173:5173

##### Production Stage
FRONTEND_DOCKERFILE=Dockerfile
FRONTEND_CONTAINER_NAME=frontend-prod
FRONTEND_COMMAND="nginx -g 'daemon off;'"
FRONTEND_PORT_MAPPING=8080:80